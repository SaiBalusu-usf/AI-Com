# EXPERIMENTS — append-only run log

Format: `date | run name | config | key metrics | what changed | decision/next step`.
Failed runs and dead ends are logged with the same care as successes (§9.3).

---

2026-07-11 | env-probe | n/a | n/a | Verified execution environment before Phase 0: CPU-only (4 cores, 15 GB RAM, no CUDA). Egress policy allows PyPI/GitHub only; cricsheet.org, kaggle.com, huggingface.co, download.pytorch.org all return 403 policy denials (recorded by the proxy). Default PyPI `torch` resolves the full CUDA-13 wheel stack (cudnn/cublas/nccl/triton — multi-GB), exceeding the §9 ask-before->2GB rule with no human available. | First facts of the project. | Decision: do NOT install torch in-session; split requirements into core/ml/cuda; every torch-dependent module gets guarded imports + `needs_torch`-marked tests; fixture data must be generated in-repo (synthetic, Cricsheet-schema) rather than downloaded. Human decisions D1–D3 recorded in PLAN.md.

2026-07-11 | seeding-bugfix | n/a | n/a | First implementation of `rng_for` used Python's builtin `hash()` for per-ball RNG keys; str hashing is salted per process, so "deterministic" template picks would have differed between runs. Caught before any experiment depended on it; replaced with sha256. Regression test added (tests/test_seeding.py). | Iteration evidence: a reproducibility bug in the reproducibility utility. | Keep sha256 keying; never use builtin hash() for anything persisted.

2026-07-11 | phase-1-data | configs/data.yaml | make data: 48 synthetic matches -> 11,388 balls -> 8,292 rows (train 6910/val 685/test 697, by match); fixture: 200 rows, 6 matches, splits 4/1/1, all event types covered | Built ingest (Cricsheet schema), features v1 (CRR/RRR/phase/momentum/salience/tension), linearize (flat+context, game-state toggle), splits-by-match, cleaning, seeded synthetic simulator + reference writer. Two implementation failures caught by tests and fixed: (1) death-phase outcome probabilities summed to 1.008 -> normalize-at-load with typo guard; (2) two wrong test expectations (balls-faced semantics around wides; record indexing across innings). Exact-dedup drops 27% of synthetic rows (short wide/dot lines repeat verbatim) — acceptable for pipeline data, flagged in DATASET_CARD as much rarer in real commentary. | D1 (real parallel corpus) remains the blocking human decision; all sources egress-blocked in this environment. Next: Phase 2 harness.

2026-07-12 | phase-2-harness | configs/eval.yaml | Harness end-to-end on fixture with dummy copy-the-input generator: scorer reference-validation rho=0.784 (n=200; per-phase powerplay 0.65 / middle 0.77 / death 0.86, heuristic-only mode), copy-gen hallucination 0.0, BLEU 0.13 vs references (expected ~0 for non-English input strings), diversity + latency computed; BERTScore and spaCy NER recorded as unavailable with reasons (blocked HF/model downloads). | Built factcheck, excitement, diversity, surface, latency, report + harness orchestrator. Five modules implemented by parallel subagents against written specs, then all modules adversarially reviewed by three independent reviewer agents. | Review findings and fixes logged in the next two entries; excitement runs heuristic-only in this environment per PLAN.md R4 (mandatory validation still enforced).

2026-07-12 | eval-modules-review | n/a | reviewer findings: 2 medium, 9 low | Fixes applied from module review: (1) surface.py bert_score guarded import now catches OSError (torch broken-install path) and mode=require wraps it in MetricUnavailable; (2) report.py "Metrics not computed" section now recognises the harness's {"status":"blocked"} sentinel — previously a blocked excitement calibration would render as "all metrics computed"; (3) latency.py rejects negative warmup_calls (negative slicing silently warmed up the wrong rows); (4) diversity.py repetition rate: verbatim-identical short outputs ("no run" x3) previously scored repetition 0.0 because they were shorter than the 4-gram window — now identical texts count as repetitive and impossible pairs leave the denominator; (5) self-BLEU now averages both directions (sentence-BLEU is asymmetric via brevity penalty) and clamps the float-epsilon 1.0000000000000004; (6) repetition metric key now derives from the configured n; (7) excitement multiword intensifiers match across any whitespace; weights validated; model arousal clamped to [0,1] for potential multi-label classifiers. | All fixes regression-covered; suite 163 green.

2026-07-12 | factcheck-v1-review | n/a | reviewer: 9 high, 3 medium, 2 low wrong-verdict classes, all with repro sentences | v1 -> v2 rewrite of the fact checker after adversarial review. v1 false-violation classes on everyday commentary: requirement talk ("they need two more to win" -> runs claim), team/partnership fifties ("fifty up for the Chargers" -> personal milestone), "six wickets in hand" -> boundary claim, bowler figures ("2 for 45" -> team-score violation), absence phrasing ("ten balls without a boundary"), idioms ("caught in two minds", "stumped for answers", "it falls safe", "that's gone, way into the stands"), fielder/next-over mentions flagged as misattribution ("Sharma at cover cuts it off", "Boult will bowl the next over"), prospective maidens; plus broken per-slot precision accounting (possible negative precision via slot-name mismatch) and plural-boundary blind spots ("back-to-back sixes" -> false omission). v2: requirement-context windows, personal-only milestone patterns, both scoreboard conventions accepted for score claims with figures-like mismatches skipped, actor-position attribution with verb lexicon + by-credit tied to wicket claims, maiden checks only on completed overs, precision booked per claim slot with separate violations_by_type, plural forms count as mentions not claims. Verified: fixture references score hallucination 0.0000 under v2; template baseline keeps its zero-hallucination property. References show omission_rate 0.159 (human-style lines like "into the crowd!" never say "six") -> added faithfulness.reference_baseline to the harness so system omission is read against the reference rate, not against 0. | 20 regression tests added from review repros (factcheck suite now 56 tests). Remaining documented recall gaps: team-milestone claims unchecked, "bowled him round his legs" idiom unhandled — precision-first policy.

2026-07-12 | phase-3-baselines | configs/baseline_template.yaml, configs/baseline_fewshot.yaml | B1 on synthetic test split (697 rows): hallucination 0.0, omission 0.0, faithfulness 1.0; excitement calibration rho vs salience*tension computed; latency p50 well under 1 ms/ball (template). B1 first attempt showed omission 0.0149 — the harness caught two REAL defects: (a) fact-checker's bowled-pronoun rule ignored sentence boundaries ("Bowled him. R Wadekar goes." -> no wicket claim), (b) the template said only "No ball from X." when the batter hit a boundary off the no-ball — a true generator omission. Both fixed with regression tests (checker + template); run regenerated. | B2 (few-shot Qwen2.5-1.5B-Instruct, hf_local): attempted via make baseline, generator build failed as expected in this environment — "hf_local backend needs the ML stack (make setup-full): No module named 'torch'". Blocked by D2/D3 (PLAN.md): torch not installable in-session, huggingface.co egress-blocked. Command ready for student hardware: `make setup-full && python scripts/run_experiment.py --config configs/baseline_fewshot.yaml`. | Phase 3 gate: B1 run dir complete + mini golden path 1.3s; B2 plumbing fully unit-tested (prompt assembly, stratified seeded example selection, spend-approval guard), execution pending unblocked hardware.

2026-07-12 | phase-4-finetuning | configs/finetune_t5.yaml, configs/finetune_qwen.yaml | no training executed in this environment (BLOCKED-ENV, decisions D2/D3) | Wrote M1 (flan-t5-base LoRA r=16 via peft/Seq2SeqTrainer, early stopping on val loss) and M2 (Qwen2.5-1.5B-Instruct QLoRA via TRL SFTTrainer, fixed commentator system prompt) with a documented degradation ladder: quantize_4bit only on CUDA+bitsandbytes, otherwise full-precision with an explicit note (resolve_device_plan, unit-tested); CPU machines substitute Qwen2.5-0.5B via the A3 ablation config. Pure-python parts (seq2seq/chat example formatting) unit-tested everywhere; training/inference raise GeneratorUnavailable("...make setup-full...") here — verified: `make train-t5` prints NOT RUNNABLE with the reason, `train_model.py --smoke` exits 4 loudly. | Phase-4 gate (50-step smoke on fixture) deferred to unblocked hardware: `make setup-full && make train-t5-smoke && make train-qwen-smoke`, then full runs need --yes (>30 min on CPU, §9.5). Evaluation path ready: run_experiment --set model.adapter_dir=<run>/adapter.

2026-07-12 | phase-5-ablations | configs/ablations/ (16 cells + README) | Executed cells (synthetic test split, 697 rows): A2-template game-state ON rho=0.540 (death overs 0.692) vs OFF rho=0.046 (per-phase undefined where register never varies) — the RQ2 mechanism measurably works for the rule-based system: tension-conditioned register selection is what produces excitement calibration. 3-seed machinery proven on B1: rho = 0.540/0.540/0.540 for seeds 13/17/23 (template variance is state-driven by design; the aggregation path for mean±std is exercised end-to-end). Faithfulness unchanged (0.0 hallucination in all cells) as expected for slot-filled templates. | Full grid defined one-file-per-cell via config `extends`; A1/A2 cells re-linearize at runtime so every cell shares identical underlying data. Model cells (A1/A3/A4/A5 on M1/M2/B2) are BLOCKED-ENV pending D2/D3 — commands in configs/ablations/README.md. | SYNTHETIC-DATA CAVEAT stands: these numbers validate pipeline + mechanism, not paper claims. Next: tables/figures.

2026-07-12 | phase-6-results | scripts/make_tables.py, scripts/make_figures.py | main_results.md/.tex generated from 5 run dirs -> 2 aggregated systems (b1_template as 3-seed mean±std, b1_template_nogamestate); all four §7 figures generated at ACL 3.3in as PDF+PNG. | Figures plot ONLY real runs (missing systems are absent, never imputed). First render review caught: legend markers readable as data points (moved to framed legend), overlapping run-name labels (seed-group dedup + staggered offsets), single-hue legend hiding run identity in the calibration scatter (per-run fixed-slot colors). Calibration figure recomputes per-ball heuristic excitement from generations.jsonl + the run's recorded dataset/split, alignment-checked. | Tables/figures regenerate with `make tables figures`; will pick up model runs automatically once D2/D3 unblock.

2026-07-12 | phase-7-final-review | n/a | reviewer: 2 high, 4 medium, 6 low across data/models/scripts | Final adversarial review before push; all substantiated findings fixed. HIGH: (1) A1/A2 fine-tune cells would have trained on stored (flat) inputs but evaluated on re-linearized ones — train_model.py now applies the identical linearization hook; (2) runtime re-linearization rebuilt prev-ball context from post-cleaning survivors (37% of context rows carried factually wrong prev= outcomes) — rows now store `prev_outcomes` captured before cleaning, and tests prove runtime == full-rebuild equality. MEDIUM: simulator strike rotation on wides/no-balls was inverted vs the laws of cricket (rotates on runs-run now; law-compliance test over 6 matches); per-delivery RNG keys collided for wides vs the next legal ball (13 byte-identical template outputs, references silently deduped away) — new unique record.delivery_seq keys all streams and is emitted in generations.jsonl; `make eval` (eval-only) read the wrong dataset for --data runs, checked alignment by count only, and destroyed measured latency on rewrite — now uses the run's recorded provenance, checks per-row identity, and carries latency forward with a note; the §9.5 long-run estimate undercounted true cost ~2x (latency pass regenerates) — plan line now reports total call count. LOW: config `_config_path` after extends named the parent file; --set overrides silently clobbered scalar nodes (now ConfigError); tables mark partial seed-group aggregates with '*'; slot figure dedups/disambiguates run names; calibration figure annotates rho recomputed from the plotted heuristic scores; tension no longer reports max pressure on a chase already won; momentum deliveries-vs-legal-balls semantics documented; Makefile plan targets no longer die on non-ML machines. | Dataset/fixture/runs regenerated post-fix (rows 8292->8303, dupes 3096->3085 — the wide-reference collisions were real data loss); RQ2 result stable: calibration rho 0.549 +/- 0.000 (3 seeds) vs 0.046 without game-state; fixture regeneration byte-identical; 171 tests green. | Repo ready for D1-D3.

2026-07-12 | d1-corpus-wiring | configs/data.yaml (source.kaggle_commentary) | loader tests 6/6 green; full suite 177 green | D1 resolved per human go-ahead. Researched candidates via web search (kaggle.com/huggingface.co pages 403 from this environment, so declared licences could not be read here — recorded as a fill-at-download field in DATASET_CARD). Chosen: primary = Kaggle "IPL 2024 Ball-By-Ball Commentary Dataset" (amitkumarbhowmick, full-season scale for fine-tuning), secondary/held-out domain = "Asia Cup T20 2022 - Ball by Ball data & commentary" (balabaskar, the corpus named in the project brief; provenance reportedly Cricbuzz per public users of the data). Built data/kaggle_commentary.py: schema-agnostic column map (defaults cover batsman/striker/batter conventions, combined "over_ball" scoreboard notation AND separate 1-based over columns with an explicit override), CSV -> Cricsheet-format conversion so the audited ingest math derives all cumulative state, reference texts keyed by delivery_seq (a wide and the following legal ball share over.ball but never share a text — regression-tested), typed extras with unknown-type counting, and a mandatory alignment audit that fails loudly above a configurable entity-mismatch rate (catches wrong column maps before they poison a faithfulness study). build_rows join key migrated from (over, ball) to (over, delivery_seq). | Remaining for the human: `kaggle datasets download -d amitkumarbhowmick/ipl-2024-ball-by-ball-commentary-dataset -p data/raw/kaggle --unzip`, glance at the licence field on each dataset page and fill the DATASET_CARD table, set source.kind=kaggle_commentary + commentary.source=kaggle_ipl2024, run `make data`. D2/D3 unchanged.

2026-07-12 | d2-ml-stack | requirements-ml.txt | install OK: torch 2.13+cu130 (CPU-only runtime), transformers 4.57.6, peft 0.19.1, trl 1.8.0, bert-score; ~5 GB disk; suite 171 passed / 7 correctly-flipped skips | Human approved the >2 GB install ("proceed"). Verified the degradation ladder LIVE with the stack present but weights egress-blocked: excitement auto-mode attempts the emotion model and falls back to heuristic recording the connection failure as fallback_reason; bert_score auto records unavailable naming the roberta-large fetch failure; `make mini` stays ~6 s under HF_HUB_OFFLINE=1. spaCy en_core_web_sm: single download attempt -> 403 on the GitHub release asset (egress policy), logged, not retried. | Remaining weight-dependent paths (B2, M1/M2 real bases, BERTScore, NER) need huggingface-reachable hardware.

2026-07-12 | d3-smoke-gate | configs/smoke/tiny_t5.yaml, configs/smoke/tiny_causal.yaml | T5 path: 50 LoRA steps on fixture, train_loss 7.26, adapter saved; causal TRL path: 50 SFT steps, train_loss 6.69; adapter reloaded and evaluated through the identical harness (200 fixture rows): hallucination 0.0, omission 1.0, p50 80.6 ms/ball CPU | Phase-4 gate machinery PROVEN without huggingface access: scripts/make_tiny_model.py builds from-scratch tiny random T5/Llama (~290k/251k params) + a BPE tokenizer trained on the fixture; `make train-t5-smoke-tiny` / `train-qwen-smoke-tiny` run the real Trainer loops. Two genuine bugs caught by executing the loop: (1) TRL 1.8 SFTConfig defaults bf16=True which transformers rejects on CPU-only -> precision now follows resolve_device_plan (bf16 iff CUDA); (2) from-scratch fast tokenizer emitted token_type_ids which T5.generate rejects -> tokenizer declares model_input_names + defensive pop in FinetunedGenerator. The random model babbles a squad surname ("Jennings" x n): hallucination 0.0 with omission 1.0 — a live demonstration that faithfulness = 1 - hallucination alone would crown a noise generator, which is exactly why the harness reports omission alongside (paper methods material). smoke_* runs are excluded from main tables/figures by default (--include-smoke to see them). | Full-weight training still needs HF-reachable hardware; commands unchanged (make train-*-smoke, then --yes runs).

2026-07-12 | machinery-smokes-complete | configs/smoke/tiny_fewshot.yaml, tiny_t5.yaml (+A1 overrides) | B2 hf_local loop live on tiny random causal model (200 fixture rows): hallucination 0.22, omission 0.90 — the parroting regime: a noise model that copies player names/outcome words from its 8-shot prompt makes checkable-and-WRONG claims, and the fact checker catches them. Together with B1 (0.0/0.0) and the pure-noise seq2seq smoke (0.0/1.0), all three metric regimes are demonstrated live. A1 parity proven: tiny T5 trained with linearization.format=context (train_model logs the re-linearization), evaluated with the identical config — all 200 eval inputs verified to carry the prev= prefix. make repro green post-install (19.7 s, fixture byte-identical). | Also: untracked 32 trainer checkpoint metadata files and gitignored checkpoint-*/; tiny Llama rebuilt with 2048 positions so 8-shot prompts fit. | Environment has now yielded everything it can: remaining work (real corpus download + licence note, real-weight smoke + full training, BERTScore/NER) needs kaggle/huggingface-reachable hardware; every command is staged in PLAN.md and README.

2026-07-12 | fill-remaining-gaps | .github/workflows/ci.yml, scripts/audit_factcheck.py | annotation-kit tests 3/3; suite 174 green | Filled what remains fillable without kaggle/huggingface access: (1) CI workflow — every push runs make setup/test, verifies committed-fixture byte-determinism, runs make repro at fixture scale, uploads tables+figures as artifacts (ML stack deliberately absent: torch tests skip, weight metrics record unavailable — the degradation ladder is CI-verified forever). (2) Checker-accuracy protocol (the one genuinely-missing research task flaggable to reviewers): scripts/audit_factcheck.py draws a seeded stratified annotation sheet from any run dir (oversampling checker-flagged rows), human fills two y/n columns, --score reports per-task precision/recall/F1/accuracy/Cohen's kappa overall and per stratum; wired as make annotate / annotate-score; scoring math unit-tested against a hand-computed confusion matrix; sampling verified deterministic and stratified against the committed parroting-smoke run. Kaggle licence cells stay fill-at-download: two more search passes confirmed the licence field simply is not exposed outside kaggle.com (page fetches 403) — honest gap, one glance at download time. | Human protocol suggestion: annotate ~100 rows per headline system once real runs exist; report kappa in the paper's harness section.

2026-07-12 | paper-scaffold-and-examples | scripts/make_examples.py, paper/ | qualitative_examples.{md,tex} generated: 5 categorized balls (wicket/six/four/death-dot/tense-chase) x 2 systems with per-output checker verdicts | Added the last two research-adjacent deliverables producible here: (1) make examples — script-generated qualitative comparison table keyed by (match, innings, over, delivery_seq) across runs sharing a data/split, checker verdict inline (papers need this table; §10 forbids hand-made ones); (2) paper/ — ACL-shaped LaTeX scaffold whose every table/figure enters via `make -C paper assets` from the generated artifacts; section stubs point at the EXPERIMENTS.md entries that carry each section's content; prose explicitly left to the human per the brief. pdflatex absent here — compile locally after dropping in the official ACL style. | Repo is now feature-complete for this environment; remaining work is exclusively the kaggle/huggingface-dependent execution path staged in PLAN.md.

2026-07-13 | windows-portability | Makefile, utils/io.py, .gitattributes | suite 174 green; fixture regeneration still byte-identical | First run on the student's Windows machine surfaced three POSIX assumptions: the Makefile's venv auto-detect shells out to `[` and falls back to python3 (neither exists on Windows), and PowerShell 5 rejects `&&`. Fixes: OS=Windows_NT branch defaults PYTHON=python (venv-activated), write_jsonl pins newline="\n" (Windows CRLF translation would have silently broken the committed fixture's byte-determinism on first `make fixture`), .gitattributes forces LF for jsonl/py/yaml/Makefile so checkout can't convert them either. ablations/clean targets remain POSIX-shell (documented: Git Bash/WSL). | Student path on Windows PowerShell: activate venv, `git pull`, `make setup`, `make repro`.

2026-07-13 | windows-utf8 | src+scripts text IO | student's Windows run: 174 tests green, make repro reached tables, fixture regenerated from 1101 candidates (matches Linux exactly — cross-platform determinism confirmed), and the FULL metric stack ran for the first time with real models (BERTScore roberta-large + emotion classifier downloaded; mini ~33 s) | make_tables crashed on Windows: Path.write_text uses the locale codec (cp1252) which cannot encode the down-arrow/rho characters in the generated tables — and the same default would have crashed on any non-ASCII player name in the real Kaggle corpus. Fixed by pinning encoding="utf-8" on every runtime text read/write across src/ and scripts/ (jsonl, yaml, json, md, tex, csv). | Verified here: suite green, tables regenerate, fixture md5 unchanged. Student: git pull, re-run make repro.

2026-07-13 | windows-legacy-encoding-guard | utils/io.py, tests/test_io.py | student run: 173/174 (one failure), root-caused | The student's pre-UTF-8-pin `make repro` had regenerated the fixture through cp1252 (the synthetic em-dash "—" became byte 0x97); git pull left the dirty file in place, and the new strict UTF-8 reader then refused it inside the annotation-kit test. Correct behaviour, terrible error message — read_jsonl now raises an actionable ValueError naming the file, the likely cause (pre-pin locale-codec write), and the remedies (git restore / make fixture / delete the stale run dir). Round-trip + legacy-byte + bad-json tests added (tests/test_io.py). | Student remedy: `git restore data/fixtures/mini.jsonl`, delete the one pre-fix local run dir, re-run make repro.

2026-07-14 | windows-green | student machine (Windows 11, CPU) | make repro fully green: 177 tests, fixture regenerated byte-identically (1101 candidates — matches Linux), mini run scored through the COMPLETE metric stack for the first time on any machine (BERTScore roberta-large + emotion-model excitement mode live), tables + figures written | Cross-platform port complete after three review cycles (POSIX Makefile assumptions, cp1252 text IO, stale locale-encoded artifacts). Follow-up: `make repro` now includes `make data` so the calibration figure has its scatter points on a fresh clone (committed runs reference the gitignored processed dataset; the rebuild is deterministic so run/dataset alignment is preserved). | Project state: everything green on both platforms; next milestone is the real Kaggle corpus (D1 download + licence note) then B2/M1/M2 real-weight runs.
